This is a modified version of the plugdata repo. I added a few externals. See the commits in the RVG branch.

To compile and install:

```
mkdir Build 
cd Build
cmake -GNinja ..
ninja
sudo ninja install
```
